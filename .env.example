# TarkovTracker Self-Hosted Configuration
# Copy this file to .env and customize

# JWT Secret - CHANGE THIS IN PRODUCTION!
JWT_SECRET=your-super-secret-key-change-me

# API URL (used during frontend build)
# For Oracle VM, use your server's public IP or domain
API_URL=http://your-server-ip:3001

# Optional: Database path (default: /data/tarkovtracker.db in Docker)
# DB_PATH=/custom/path/to/database.db
